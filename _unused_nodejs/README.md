# Unused Node.js Files

These files are NOT used in the current application.

The application runs entirely with **vanilla JavaScript** in the browser.

## Files in this folder:
- `server.js` - Express server (not used)
- `package.json` - Node.js dependencies (not used)
- `.env` - Environment variables (not used)

## Current Setup:
The application uses:
- ✓ Vanilla JavaScript (ES6 modules)
- ✓ Client-side API calls (keys in `scripts/config.js`)
- ✓ Leaflet.js for maps (CDN library, not a framework)
- ✓ No build tools or frameworks

To run the app, simply open `index.html` in a browser or use a simple HTTP server.
